<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Debug-only inspector driven over adb: dumps the Compose semantics tree, taps by tag, types text. -->
        <receiver android:name="app.aoide.debug.Inspector" android:exported="true">
            <intent-filter>
                <action android:name="app.aoide.DEBUG" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
